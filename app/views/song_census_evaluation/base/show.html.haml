= content_for(:toolbar, render('actions_index'))
= render 'shared/page_per_year'
= render 'infos'

#main.row-fluid
  - if @total
    %article.span8
      = render 'totals'

  - else
    %p= t('.no_data_for_this_year')
