= render 'shared/page_per_year'
= render 'infos'

#main.row-fluid
  = text_field_tag('q', nil,
                  placeholder: I18n.t('.search'),
                  data: { provide: 'entity',
                          updater: 'Songs.update',
                          url: songs_path })

