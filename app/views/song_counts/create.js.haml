$(".song-counts :input[data-updater='Songs.update']")[0].value = '';

- if entry.valid?
  $('.song-counts .list').html("#{j(render("list", year: entry.year, list: @list))}");

$('#flash').html("#{j(render(partial: "layouts/flash", collection: [:notice, :alert], as: :level))}");
